# 2026-07-17

## Today's Goal

- New-day status: Phase 1a complete — confirm Phase 1b TTS as next major workstream
- Commit/push journal housekeeping
- Start Phase 1b TTS foundation

**Outcome:** Journals pushed (`3b3edef`). Phase 1b scaffold shipped and pushed (`5e418db`): TTS provider abstraction (stub + OpenAI), character `tts` profiles, `SpeakVoiceAction` on `ResponseReadyEvent`, CLI `--tts` / `--tts-play`, 51 tests green.

*Session end (scaffold) — goals for today met; no further goals after the TTS scaffold.*

- Debrief: walk through how Phase 1b TTS is handled (no new implementation)

**Outcome (debrief):** Confirmed understanding of optional Act/Voice layer on `ResponseReadyEvent`; no code changes this turn.

*Session end — status/debrief only after the scaffold.*

- Help user test TTS from project directory (commands only)

**Outcome (TTS test guide):** Confirmed local env ready (`.venv`, `OPENAI_API_KEY`, `character-os` on PATH). Gave stub then live `--tts --tts-play` commands. No agent-side live listen verification this turn; awaiting user results.

*Session end — test guidance delivered; live OpenAI+play result not yet recorded.*

- Record live OpenAI TTS verification + voice-quality feedback

**Outcome (live verify):** User confirmed TTS works end-to-end — text reply, `[tts]` path, `.mp3` under `data/audio/…`, playback heard. Voice is male (`onyx`) but monotone / near-robotic, not pirate-theatrical. Agreed this is Phase 1b polish (YAML instructions / voice id), not Phase 2/3.

*Session end — live verify closed; next work is voice-character tuning.*

- Tune Redbeard TTS for less monotone delivery (`cedar` + stronger instructions)

**Outcome (voice tune):** Switched Redbeard `tts.voice` `onyx` → `cedar`, speed `0.95`, rewritten `instructions` for affect/intonation/anti-monotone. Awaiting user `--tts-play` retest.

*Session end (voice tune) — first-pass YAML polish done; human listen retest not yet reported; character.yaml + test changes still local/uncommitted.*

- Cedar retest + decide: refine Redbeard vs new character

**Outcome (cedar retest):** User reports different voice but still pretty monotone; wonders if pirate is too advanced. Recommendation: keep Redbeard; simplify vocal brief (characterful gravelly male, emotion/pitch variation, **no dialect parody**) before adding a second easier-voice character. Not implemented this turn — awaiting user go-ahead.

*Session end (cedar retest) — pipeline still good; voice-character strategy agreed in discussion only.*

- Apply simplified Redbeard vocal brief (no dialect parody)

**Outcome (simplify brief):** Rewrote `tts.instructions` — natural gravelly male storyteller with prosody/emotion; explicit ban on pirate dialect/accent parody. Speed back to `1.0`. Awaiting `--tts-play` retest.

*Session end (simplify brief) — YAML applied; human listen result not yet reported; character.yaml + tests + journal still local/uncommitted.*

- Simplify-brief retest: natural but bland; push character without dialect

**Outcome (character color):** User: sounds like a normal man — no character. Retuned to `ash` @ 0.92 with grit/swagger/emotion instructions; dialect still banned. Awaiting retest. Plan B new character remains if this middle path fails.

*Session end (character color) — middle-path YAML applied (`ash` + grit); listen result not yet reported; edits still local/uncommitted.*

- Ash/grit retest: better prosody but “bad pirate impression”; soften speech orthography

**Outcome (anti-impression):** Diagnosis: pirate spellings in dialogue (`Arrr`/`ye`/`fer`) push TTS into a failed pirate act. Added `tts.normalize_speech` + `voice/speech_text.py` (speech-only); instructions now ban pirate impression explicitly. Printed CLI text unchanged. Awaiting retest.

*Session end (anti-impression) — normalizer + instruction rewrite applied; listen result not yet reported; voice/TTS edits still local/uncommitted.*

- Plan B: create companion robot character **Lumen** + world `everyday-present` with calm TTS (`sage`)

**Outcome (Lumen pack):** Original soft-poly household companion pack added (Baymax-inspired vibe, not a clone). World `everyday-present` with companion norms / human patterns / home context. TTS retuned after user feedback: `cedar` @ 0.88 with slow/deliberate/restrained-compassion instructions. Awaiting retest.

*Session end (Lumen pack) — Plan B character shipped locally/uncommitted; live Lumen TTS not yet heard.*

- Retune Lumen TTS to `cedar` + deliberate calm vibe

**Outcome (Lumen TTS retune):** Voice `sage` → `cedar`, speed `1.05` → `0.88`, instructions rewritten to user's brief (slow, deliberate, steady pitch, restrained compassion). Awaiting `--tts-play` retest.

*Session end (Lumen TTS retune) — cedar/deliberate profile applied; listen result not yet reported; still uncommitted.*

- Status check after Lumen cedar accept: where next?

**Outcome (status):** User accepts Lumen/`cedar` as “sounds like a robot companion — not great but gets the job done.” Phase 1b MVP treated as demo-ready with Lumen as voice reference; Redbeard remains hard-mode. Immediate next: commit+push uncommitted 1b stack; then optional 1b polish or Phase 2 (Studio).

*Session end (status) — 1b MVP accepted; housekeeping commit is the blocking next step.*

- Commit + push Phase 1b stack

**Outcome (commit):** Pushed `ee7f10f` — Lumen + `everyday-present`, speech orthography normalize, Redbeard TTS tunes, docs/tests (54 passed). `feature/dev` matches origin. Phase 1b MVP on remote.

*Session end (commit) — 1b stack shipped; day closes with MVP on origin.*

- Post-commit status: what is next?

**Outcome (next-steps):** Recommended **Phase 2 Character Studio** as the next major workstream; optional 1b TTS polish remains available but non-blocking. User choice pending. Trailing journal notes still local-only.

*Session end (next-steps) — awaiting user pick: Phase 2 vs more voice polish.*

- Polish voice (emotion overlays + non-blocking interruptible playback)

**Outcome (voice polish):** Added drive-based TTS instruction overlays (`emotion_overlay`), non-blocking playback that interrupts prior clips, session `close()` stops audio. Lumen micro-tune: speed `0.90`, anti-flat wording. Awaiting user retest.

*Session end (voice polish) — polish implemented locally (56 tests); live listen not yet reported; uncommitted vs `ee7f10f`.*

- Debrief Lumen food chat; fix dialogue coherence (not prior-session memory)

**Outcome (Lumen dialogue):** User clarified air-quality/wildfire opener was **prior-session memory**, not invention — correction noted. Real issues: coachy re-asks and mid-session forget of the burger order. Fixed: wire `recent_dialogue` into response prompt, anti-coach rules, Lumen prompt overrides; anti-invention rules explicitly allow Known facts / prior memory. Retest recommended for coherence, not for “forgetting wildfires.”

*Session end (Lumen dialogue) — misdiagnosis corrected; coherence fixes local/uncommitted with voice polish stack.*

- Second Lumen live chat: still interview-shaped; tighten anti-question rules

**Outcome (anti-interview):** New transcript still opens many turns with soft-coach phrasing and ends almost every reply with a question; also dragged healthy-eating goal after user said indoor plans were unrelated. Strengthened Lumen prompts: default no-question, ban "It sounds like", don't force old goals; lowered curiosity drive 0.75→0.55. Awaiting retest.

*Session end (anti-interview) — harder companion rules applied locally; live verify pending; note persisted SQLite curiosity may still be old until reset.*

- Third Lumen live chat after anti-interview: fewer questions (good); still bland + assistant-y

**Outcome (anti-bland):** User retest showed statements without interview quizzes — progress. Remaining: filler openers ("That sounds like…"), vague filmmaker filler, and goal answers framed as helpful/supportive assistant. Tightened Lumen prompt: ban "That sounds like", demand concrete details, goal talk = understand/remember not support/help; demoted help goal to low. Awaiting another retest.

*Session end (anti-bland) — interview habit largely fixed; bland/assistant tone pass applied locally; live retest of concreteness + goal phrasing still pending; uncommitted polish stack remains.*

- Anti-bland retest after restart (goal / companion questions)

**Outcome (anti-bland retest):** Goal answer used understand/remember framing with concrete recall (healthy eating, bacon cheeseburgers); no interview questions; no help/support product language. Mild leftover soft closer (“adds to our connection”) and food-fact repetition — judged good enough to commit polish stack unless another tone pass is requested.

*Session end (anti-bland retest) — companion goal phrasing accepted; commit of local polish+dialogue stack is the blocking next step.*

- Commit + push polish+dialogue stack

**Outcome (commit):** Committed and pushed `0ad03d7` — voice polish (emotion overlay, interruptible playback), `recent_dialogue` + per-character prompt overrides, and Lumen anti-interview/anti-bland companion prompts. 57 tests green. `feature/dev` matches origin.

*Session end (commit) — polish+dialogue stack on origin/feature/dev (`0ad03d7`); next fork is Phase 2 or optional streaming TTS.*

*Session end — day/session close: journal reviewed; no further work this turn beyond documenting the push.*

- Post-polish status: what is next?

**Outcome (next-steps):** Phase 1b treated as closed (MVP + Lumen polish on `0ad03d7`). Recommended next major workstream: **Phase 2 Character Studio**. Optional non-blocking leftovers: streaming TTS, mild Lumen soft-closer micro-pass, SQLite curiosity reset if `/state` looks wrong. STT remains Phase 3. User choice still pending.

*Session end (next-steps) — awaiting user pick: Phase 2 vs optional 1b leftovers.*

- Pre–Phase 2: agent-driven text probe / refine loop

**Outcome (probe plan):** User wants to delay Phase 2 and keep testing current brain: reset memory, text-only (no TTS), agent runs conversations, inspect thoughts/state/responses, tweak packs, repeat. Confirmed feasible with existing CLI/API (`--no-persist` or wipe SQLite; omit `--tts`; multi-turn `CharacterSession` + `--show-thoughts` / `/state` / `--debug-stages`). `/forget` is not a full wipe. Implementation of the probe loop not started this turn — awaiting user go-ahead on first scripted Lumen run.

*Session end (probe plan) — Phase 2 deferred; agent text probe agreed in principle; first run not yet executed.*

- First Lumen agent text probe (evaluator framing)

**Outcome (lumen probe):** Ran fresh Lumen OpenAI text-only session (`persist=False`, no TTS). Opening framed agent as Character OS evaluator learning from Lumen's interaction/thinking to improve it. Three turns saved to `data/probe/lumen_agent_transcript.json` (gitignored under `data/*`). Memory captured role + preferences (quiet evenings, bacon cheeseburgers, Nolan). Trust/familiarity rose slowly (0.47/0.08 → 0.54/0.14). Issues noted: turn-1 soft question; internal thoughts invent text-impossible body language; tone still mildly analytical. No pack tweaks applied yet — awaiting continue vs fix-first.

*Session end (lumen probe) — first probe complete; next is continue conversation or tighten thought prompts.*

- Continue Lumen probe + harden Phase 1a foundation

**Outcome (foundation probe):** Continued agent text evaluation after fixes. Changes: (1) thought prompts ban invented body language; (2) Lumen meta-evaluator replies must not ask what to study; (3) cautious/building rapport stance less quiz-pushy; (4) bugfix — `dog's name is X` was mis-canonicalized to `The user's name is X` via `_NAME_RE` / dedupe on load. Fresh 8-turn foundation probe (`data/probe/lumen_foundation_probe.json`): **0/8 questions, 0/8 body-cue thoughts, 0/8 assistanty phrases**; trust 0.47→0.51, familiarity 0.08→0.18; goals/memory/redirect/Nolan recall behaved well. Cross-session SQLite persist retest: seafoam + Pixel survive; pet-name corruption gone. 58 tests green. Phase 1a foundation judged **strong enough to proceed** with minor optional polish remaining (world-pack pull still soft; occasional soft closers).

*Session end (foundation probe) — Phase 1a text brain meets success bar for Lumen; Phase 2 still optional next after user confirm.*

*Session end — journal close-out: no further goals this turn; foundation probe outcome above stands.*

- Commit + push foundation fixes; consider Redbeard spectrum probe

**Outcome (commit):** Pushed `18eb8ba` — foundation hardening (thought channel awareness, meta-evaluator no-quiz, less quiz-pushy rapport stance, pet-name memory bugfix, journal). 58 tests green. User agreed: run the same agent text probe against **Redbeard** next for hard-mode / pirate-persona spectrum coverage before Phase 2.

*Session end (commit) — foundation fixes on origin (`18eb8ba`); Redbeard spectrum probe is next.*

- Redbeard agent text foundation probe (+ anti-interview harden)

**Outcome (redbeard probe):** Fresh 8-turn text-only probe (`persist=False`). Strong pirate personality, world lore (Isla / navy / Crimson Tide / Morgana), goals, memory, and low trust (stays ~0.2 — appropriate). First pass asked on most turns (loyalty-grill loop). Added Redbeard prompt overrides + softened `size_up_stranger` goal. Retest v3: content questions **2/8**, loyalty-grill **0/8**, body-cue thoughts **0/8**, topic redirect statement-only. Rhetorical `eh?`/`savvy?` still appear sometimes (character flavor). Spectrum contrast with Lumen confirmed. Changes local/uncommitted.

*Session end (redbeard probe) — hard-mode end of spectrum acceptable after prompt harden; commit when user asks.*

*Session end — journal close-out: no further goals this turn; Redbeard probe outcome above stands.*

- Commit + push Redbeard spectrum harden

**Outcome (commit):** Pushed `f64e4c8` — Redbeard prompt overrides (statement-led suspicion), softened `size_up_stranger` goal, loader tests, and journal. 59 tests green. Phase 1a spectrum (Lumen + Redbeard) harden on origin.

*Session end (commit) — Redbeard harden on origin (`f64e4c8`); Phase 1a accept / Phase 2 next.*

*Session end — journal close-out after push; no further work this turn.*

- Post-spectrum status: what is next?

**Outcome (next-steps):** Phase 1a spectrum treated as complete (Lumen + Redbeard probes; hardens through `f64e4c8` on origin). Recommended next: user Phase 1a accept, then **Phase 2 Character Studio**. Optional leftovers remain non-blocking (Lumen world-knowledge pull, streaming TTS, `/reset` CLI, trailing journal dirty vs `f64e4c8`). User choice pending.

*Session end (next-steps) — awaiting Phase 1a accept → Phase 2, or optional polish.*

- Choose next workstream: accept Phase 1a and start Phase 2 Studio

**Outcome (phase-2 kickoff):** Agent chose **Phase 2 Character Studio** after treating Phase 1a spectrum as accepted. Shipped CLI MVP (`character-os-studio`): list/show characters & worlds, inspect SQLite memories/drives, scaffold `create-character`. Docs: `PHASE_2_STUDIO.md`. Design: CLI-first atop `loader/` + `persistence/`; no chat bus bypass; web UI later. Tests added under `tests/studio/`. Smoke verified: 65 tests green; `list`/`show`/`memories` work against live Lumen DB.

*Session end (phase-2 kickoff) — Phase 1a accepted by choice; Studio CLI MVP local (commit when ready).*

*Session end — journal close-out: no further goals this turn; Phase 2 kickoff outcome above stands.*

- Commit + push Phase 2 Studio CLI MVP

**Outcome (commit):** Pushed `cdd474c` — Phase 2 Character Studio CLI MVP (`character-os-studio`): list/show packs, inspect memories/drives, scaffold characters; `StudioService`; docs + tests (65 green). `feature/dev` matches origin.

*Session end (commit) — Phase 2 Studio CLI MVP on origin (`cdd474c`); next slices are edit/validate + debug tools.*

*Session end — journal close-out after push; no further work this turn.*

- Post-Studio-MVP status: what is next?

**Outcome (next-steps):** Phase 2 CLI MVP treated as shipped (`cdd474c`). Recommended next Studio slice: **in-place character edit + YAML validation** (highest authoring leverage). Stage-debug viewer and optional leftovers (streaming TTS, `/reset`, Lumen world-knowledge) remain secondary. User choice pending.

*Session end (next-steps) — awaiting pick: edit/validate (recommended) vs stage-debug vs optional polish.*

- Studio edit + YAML validation slice

**Outcome (studio edit):** Added `validate character|world` and `set character` with safe in-place edits (traits, quirks, drives, goals, name/world/description, relationship defaults). Edits rewrite YAML, keep `.yaml.bak`, and roll back if loader/validation fails. Docs + tests updated (69 green). Local/uncommitted.

*Session end (studio edit) — edit/validate slice implemented locally; commit when user asks.*

*Session end — journal close-out: no further goals this turn; studio edit outcome above stands.*

- Commit + push Studio edit/validate slice

**Outcome (commit):** Pushed `e13f7b5` — Studio `validate` + `set` with `.yaml.bak` rollback, docs, tests (69 green). `feature/dev` matches origin.

*Session end (commit) — edit/validate on origin (`e13f7b5`); next is world edit or stage-debug.*

*Session end — journal close-out after push; no further work this turn.*

- Status briefing: done so far vs remaining roadmap

**Outcome (status):** Summarized project state for the user. **Done:** Phase 1a brain (accepted; Lumen + Redbeard spectrum), Phase 1b TTS MVP, Phase 2 Studio CLI (list/show/memories/create + validate/set through `e13f7b5`). **Left:** Phase 2 world edit, richer editors, stage-debug, optional GUI; optional polish (streaming TTS, `/reset`, Lumen world-knowledge); Phases 3–5 (awareness, embodiment, experiences). Recommended next: world scaffold/edit or stage-debug. No code changes this turn.

*Session end (status) — roadmap position restated; awaiting next-slice choice.*

- Continue Phase 2: world create/edit, richer personality/goal editors, stage-debug, optional web UI

**Outcome (phase-2 expansion):** Shipped remaining Phase 2 Studio slices on one pass:
1. World scaffold (`create-world`), `set world`, `add-knowledge` with `.yaml.bak` validate/rollback
2. Richer character `set`: fears, preferences, goal priority/status/description
3. Stage-debug `trace` via `run_stage_trace` + lifecycle stage capture
4. Optional stdlib web UI (`serve`) over the same `StudioService`
Docs (`PHASE_2_STUDIO.md`, README, ROADMAP), tests, `.gitignore` for `world.yaml.bak`. Phase 2 authoring/debug surface treated as complete for roadmap goals; commit when user asks.

*Session end (phase-2 expansion) — world/richer-edit/trace/serve implemented locally; awaiting commit.*

- Journal close-out before ending session (fill/confirm all five sections)

**Outcome (journal close):** Confirmed Phase 2 expansion entries above. No further implementation this turn — session ends with Phase 2 Studio authoring/debug goals complete locally (73 tests); commit/push still pending user ask.

*Session end — journal close-out only; no new code this turn.*

- User checked Studio web UI (`serve`) — looks good

**Outcome (UI accept):** Human verify of optional Phase 2 `serve` UI accepted. No code changes. Expansion stack still local/uncommitted until user asks to commit.

*Session end (UI accept) — web UI accepted; commit of Phase 2 expansion is the natural next step.*

- Journal close-out before ending session (confirm all five sections after UI accept)

**Outcome (session close):** Day/session ends with Phase 2 expansion complete locally and `serve` UI human-accepted. No further code this turn. Blocking next step remains commit+push of the expansion stack when the user asks.

*Session end — journal close-out after UI accept; no further goals this turn.*

## What We Learned

- Roadmap / structure already lock Phase 1b scope: TTS + character voice profiles; STT deferred to Phase 3
- Docs already point at the integration shape: `behavior/actions/speak_voice.py` (or `voice/`) consuming `ResponseReadyEvent` without rewriting the text pipeline
- `personality.voice` is writing-style; speech synthesis needs a separate `tts:` block in character YAML
- TTS must subscribe to `ResponseReadyEvent` *before* the session capture handler so `SessionResult.audio_path` is populated
- Stub TTS writes a `.txt` sidecar (no audio APIs); OpenAI uses `gpt-4o-mini-tts` + optional `instructions`
- Stub LLM + stub TTS smoke works end-to-end (`--provider stub --tts stub`); audio path printed as `[tts] …`

*Session end (scaffold) — no further learnings after the scaffold push.*

*Session end (debrief) — no new learnings; restated existing TTS design only.*

- User machine already has venv + `OPENAI_API_KEY`; activate `.venv` then run `character-os --provider openai --tts --tts-play`

*Session end (TTS test guide) — no new technical learnings beyond confirming env readiness.*

- Live OpenAI `--tts --tts-play` succeeds: synthesis + save + play; path printed as `[tts] …/*.mp3`
- Default Redbeard profile (`onyx` + current `instructions`) reads as deep male but flat — expressiveness needs stronger style prompts and/or a different OpenAI voice id, still in Phase 1b

*Session end (live verify) — pipeline validated; character-of-voice is the open learning.*

- OpenAI docs recommend `marin`/`cedar` for best quality; `instructions` can drive accent, emotion, intonation, speed — monotone fix is prompt+voice selection, not pipeline

*Session end (voice tune) — no further learnings until user retests cedar delivery.*

- `cedar` + stronger pirate/accent instructions still reads monotone — accent+theatrical “pirate voice” is a hard stock-TTS ask; pirate *text* can stay while spoken delivery aims for natural emotional male storyteller
- New character is Plan B after one simpler Redbeard vocal brief fails — not the first move

*Session end (cedar retest) — key learning: separate dialogue persona from TTS vocal ambition.*

*Session end (simplify brief) — no new learnings beyond applying that separation in YAML; await listen test.*

- Non-dialect “natural storyteller” brief fixed robot/monotone but collapsed into generic everyman — TTS needs explicit *character color* (grit, swagger, emotion) without dialect parody; swinging too far either way fails

*Session end (character color) — tradeoff mapped: accent parody ↔ bland natural; middle path under test.*

- Ash/grit improved prosody (pauses, elongation, volume) but still read as “normal voice doing a bad pirate impression”
- Dialect orthography in the *spoken* string is a trigger — speech-only normalization (`ye`→`you`, drop `Arrr`, etc.) keeps CLI pirate text while feeding TTS plainer English

*Session end (anti-impression) — printed persona ≠ spoken string; that split is now code + config.*

- After Redbeard TTS stayed hard (monotone / bad pirate act), Plan B is the right call for Phase 1b demo: calm clear modern speech is a much better stock-TTS fit than dialect-heavy pirate text
- Stub LLM still returns pirate canned lines regardless of character — live OpenAI required to evaluate Lumen dialogue/voice
- New characters remain data-only (`characters/` + `worlds/`); no engine code required for Lumen beyond tests/docs

*Session end (Lumen pack) — confirmed data-driven character add; TTS ease is a pack design choice.*

- First Lumen live try: user disliked `sage` @ ~1.05 — wanted slower, more deliberate warmth; target vibe is restrained compassion (steady pitch, subtle thought-pauses, never rushed/sarcastic/enthusiastic)

*Session end (Lumen TTS retune) — companion TTS taste is pack-specific and iterate-fast via YAML.*

- Lumen/`cedar` @ 0.88 is acceptable for Phase 1b demo quality bar (“robot companion,” imperfect but usable); chasing perfect Redbeard voice is not required to close 1b MVP

*Session end (status) — quality bar set: good-enough companion voice > perfect pirate voice.*

*Session end (commit) — no new learnings; shipping confirmed remote parity for 1b MVP.*

*Session end (next-steps) — nothing new; roadmap fork is Phase 2 vs optional 1b polish.*

- Blocking `afplay` via `subprocess.run` made the CLI wait for entire clips — non-blocking Popen + interrupt is required for usable multi-turn voice chat
- Emotion overlays must stay mild for restrained companions (Lumen); append drive hints to instructions rather than rewriting the base voice brief

*Session end (voice polish) — UX lesson: playback must not block the input loop.*

- Do not assume “unexpected world detail” = hallucination — check long-term memory / prior sessions first (user correction on wildfires/air quality)
- `recent_dialogue` was computed in session context but never injected into `response_generator.md` — explains mid-session re-asks even when memory had older facts
- Companion characters need prompt overrides that allow analytical/robotic tone; global “not analytical or robotic” fought Lumen’s identity

*Session end (Lumen dialogue) — memory vs invention distinction + missing recent_dialogue were the key learnings.*

- Soft anti-coach wording was not enough — Lumen still ran an interview (question nearly every turn + "It sounds like…" openers)
- Memory of prior goals can hijack the topic if prompts don't say "don't force unrelated past goals" when the user redirects
- YAML curiosity baseline changes do not rewrite existing SQLite emotional_drives for that character

*Session end (anti-interview) — companion ≠ interviewer; need hard question-rate + opener bans.*

- Killing the interview habit ≠ companion feel: third live chat was statement-heavy but still bland/chatbot (vague praise, "That sounds like…", goal talk as "helpful" / "support you")
- Opener bans must cover close variants (`That sounds like` as well as `It sounds like`)
- Goal/purpose answers need explicit anti-assistant framing (understand/remember, not support/help) or the model defaults to product language
- Demoting `help_when_useful` to low priority reduces assistant-mode drift when the user asks about the character's goals

*Session end (anti-bland) — statement-only was necessary but not sufficient; concreteness + anti-support phrasing are the next companion levers.*

- Explicit understand/remember goal rules worked after restart: Lumen answered goals without “help/support” product language and named remembered preferences
- Remaining soft spots are mild (repeating the same food facts; warm chatbot closers) — not blockers for shipping the current prompt pass
- Restart alone does not clear long-term memory; concrete recall across sessions is expected and desirable when Known facts apply

*Session end (anti-bland retest) — anti-bland prompt pass validated enough to stop iterating and commit.*

*Session end (commit) — no new learnings; shipped validated local polish as `0ad03d7`.*

*Session end — nothing further learned this close-out.*

- Phase 1b success bar is met for demos: TTS pipeline + Lumen companion voice/dialogue polish; remaining 1b items are optional polish, not phase blockers
- Roadmap next major phase is still Character Studio (Phase 2); speech recognition stays Phase 3

*Session end (next-steps) — no new technical learnings; confirmed Phase 1b closed / Phase 2 recommended.*

- Agent can drive live character evaluation without TTS: keep one `CharacterSession`, call `send_message`, read thoughts/drives/memories between turns
- Fresh start options: `--no-persist` (ephemeral) or delete/replace `data/character_os.sqlite3`; `/forget` only archives low-importance stale facts, not a full memory reset
- Scripted multi-turn in-process beats repeated `--once` if session continuity and state deltas matter

*Session end (probe plan) — existing CLI/session API already supports an observe→tweak→retest dialogue loop.*

- Multi-turn probe continuity: replay prior user lines into a new `--no-persist` session (conversation is session-only) then append new turns; transcript JSON under `data/probe/` works as the source of truth
- Lumen stores evaluator role + concrete likes correctly after a few turns; trust/familiarity and drive curiosity/trust inch up with open sharing
- Internal thoughts can invent physical/body-language cues in a pure-text channel — thought prompts need channel awareness (“no body to observe”)
- Anti-interview is imperfect on meta/evaluator openers (turn 1 still asked); later preference-sharing turns stayed statement-only and concrete

*Session end (lumen probe) — probe loop works end-to-end; thought channel-awareness and soft first-question are the main findings.*

- After prompt + stance fixes, Lumen 8-turn probe had zero spoken questions and zero invented body-language thoughts
- Rapport stance text in `format_relationship_stance` can push interview behavior (“curiosity about the plan”) even when character prompts say otherwise — stance wording matters as much as pack prompts
- Memory bug: `_NAME_RE` treated any `name is X` as the user's name, so `dog's name is Pixel` became `The user's name is Pixel` on SQLite load/dedupe — possessive `'s name is` must be excluded
- Cross-session persistence works when facts stay correctly labeled; session-only conversation still needs replay for ephemeral probes
- Phase 1a success criteria (personality, memory, emotion drift, goals, curiosity without chatbot interview) largely met for Lumen; world-knowledge citation in speech is the weakest remaining signal

*Session end (foundation probe) — foundation gaps found were fixable in prompts/stance/memory regex; Lumen demo bar cleared.*

*Session end — nothing further learned this close-out.*

- Dual-character agent probing is the right Phase 1a gate: Lumen = calm companion end; Redbeard = suspicious pirate / hard-mode end — same pipeline under opposite social pressures
- Shipping foundation fixes (`18eb8ba`) before the Redbeard probe keeps the shared memory/stance/thought fixes under both characters

*Session end (commit) — spectrum testing (Lumen then Redbeard) confirmed as the remaining Phase 1a validation plan.*

- Redbeard hard-mode fails differently than Lumen: not soft chatbot — loyalty-grill interview + rhetorical `eh?`/`savvy?` inflate question counts
- Score content questions separately from pirate tags; after prompt overrides, content Q dropped to ~2/8 and loyalty loops to 0/8 while personality stayed strong
- Low/flat trust (~0.2) across an observer conversation is correct for this pack — do not “fix” it toward Lumen’s rising trust
- World knowledge worked (Crimson Tide, Morgana Flint, Isla, navy); `size_up_stranger` goal wording was pushing per-turn interrogation

*Session end (redbeard probe) — spectrum probe shows shared pipeline + pack-specific prompt overrides are enough to keep both ends playable.*

*Session end — nothing further learned this close-out.*

*Session end (commit) — no new learnings; shipped Redbeard harden as `f64e4c8`.*

- No new technical learnings — status only. Roadmap next major phase remains Character Studio (Phase 2) now that both spectrum ends are probed and shipped

*Session end (next-steps) — nothing new beyond confirming Phase 2 as the recommended next major workstream.*

- Phase 2 first slice can be a CLI `StudioService` over existing loaders/persistence without introducing a web stack or touching the chat event bus
- Scaffolded packs should be minimal-valid YAML; personality depth stays hand-edited in the pack (data-driven rule)

*Session end (phase-2 kickoff) — Studio = inspect/scaffold layer; brain pipeline unchanged.*

*Session end — nothing further learned this close-out.*

*Session end (commit) — no new learnings; shipped Studio CLI MVP as `cdd474c`.*

- After list/inspect/scaffold, the highest-leverage Studio gap is safe in-place pack editing with validation — hand-editing YAML blind is the remaining authoring friction
- No new code learnings this turn (status only)

*Session end (next-steps) — edit/validate recommended as the next Phase 2 slice.*

- Studio edits should validate via the real loaders and keep a `.bak` rollback — authors need safety when rewriting YAML
- PyYAML `safe_dump` will not preserve comments; acceptable for targeted `set` if documented

*Session end (studio edit) — validate-then-commit edit path is the right safety model for CLI pack mutation.*

*Session end — nothing further learned this close-out.*

*Session end (commit) — no new learnings; shipped edit/validate as `e13f7b5`.*

- Status-only: Phase 1a/1b closed for demos; Phase 2 is partial (character authoring CLI yes; world edit / debug / GUI no); Phases 3–5 untouched

*Session end (status) — no new technical learnings.*

- One `StudioService` can power CLI + stdlib web UI without a framework; stage-debug reuses `format_stage_line` + a one-shot `CharacterSession`
- World knowledge appends are file-oriented (`knowledge/*.yaml` + `knowledge_files` index), same validate-then-keep model as character `set`
- Bus `subscribe_all` sees nested publishes unwind (Act before Observe); `start_recording` preserves publish order for stage traces

*Session end (phase-2 expansion) — Studio stays thin over loader/persistence/session.*

*Session end (journal close) — nothing new learned this turn beyond confirming the Phase 2 expansion notes above.*

*Session end (UI accept) — no new technical learnings; UI acceptance only.*

*Session end (session close) — nothing further learned; prior Phase 2 expansion + UI-accept notes stand.*

## Architectural Decisions

- Prior lock stands: Phase 1b is TTS output only; speech recognition stays Phase 3; ticks remain state-only (no unprompted speech)
- New `voice/` package mirrors `llm/` (abstract provider + stub/openai); behavior stays event-driven via `SpeakVoiceAction`
- New bus event `SpeechSynthesizedEvent` for Act/Voice visibility and future subscribers
- Character packs own voice id / model / instructions; CLI/env can override provider only
- Playback is optional (`--tts-play`) via system players (`afplay` / `ffplay` / `paplay`) — synthesis always writes under `data/audio/`

*Session end (scaffold) — no new architectural decisions after `5e418db`.*

*Session end (debrief) — nothing changed; prior TTS decisions restated only.*

*Session end (TTS test guide) — nothing changed.*

- Voice character tuning stays Phase 1b / data-driven (`tts.instructions`, `tts.voice`) — not deferred to Studio or later phases

*Session end (live verify) — no new code architecture; confirmed polish belongs in character pack TTS config.*

- Keep expressiveness in character `tts:` YAML; try `cedar` before chasing custom voices or new providers

*Session end (voice tune) — Architectural Decisions unchanged beyond the cedar/YAML choice above.*

- Prefer one more Redbeard `tts.instructions` simplification over spinning a new pack mid-1b; add an easier-voice character only if simplified brief still fails

*Session end (cedar retest) — decision pending user confirmation; no YAML change yet this turn.*

- Applied: Redbeard spoken brief = natural emotional male storyteller; pirate dialect explicitly forbidden; dialogue persona unchanged in LLM prompts

*Session end (simplify brief) — Architectural Decisions unchanged except the applied vocal-brief split above.*

- Middle path: keep dialect ban; restore character via voice id (`ash`) + grit/swagger/emotion instructions; Plan B second character only if middle path fails listen test

*Session end (character color) — no pipeline changes; still data-driven TTS pack tuning.*

- `TTSProfile.normalize_speech` (pack opt-in) + `voice/speech_text.normalize_for_speech` applied in `SpeakVoiceAction` before synthesize; CLI/`ResponseReadyEvent` text unchanged
- Instructions: grit/swagger without pirate performance; Redbeard enables `normalize_speech: true`

*Session end (anti-impression) — new decision: speech orthography normalization is first-class pack config.*

- Execute Plan B: ship **Lumen** + `everyday-present` as Phase 1 reference companion; keep Redbeard as hard-mode / text-strong character
- Lumen TTS defaults: OpenAI `cedar`, speed `0.88`, deliberate/calm instructions, `normalize_speech: false`

*Session end (Lumen pack) — dual-character strategy locked for 1b demos.*

- Lumen voice locked to user brief: `cedar` @ `0.88` + deliberate/calm/restrained-compassion instructions (replacing initial `sage` @ 1.05)

*Session end (Lumen TTS retune) — no pipeline changes; profile-only retune.*

- Phase 1b MVP closure criteria: TTS pipeline + character voice profiles + Lumen demo acceptable; Redbeard TTS polish deferred as optional

*Session end (status) — Architectural Decisions unchanged beyond accepting Lumen as the 1b voice reference.*

*Session end (commit) — nothing changed; `ee7f10f` records prior 1b decisions on origin.*

*Session end (next-steps) — no new decisions; Phase 2 recommended but not yet started.*

- `TTSProfile.emotion_overlay` (default true) + `voice/emotion_overlay.py`; `SpeakVoiceAction` merges overlays via `get_drives` from session state
- Playback: background process with `stop_audio()` on new speech and session close; CLI no longer blocks on `--tts-play`

*Session end (voice polish) — new decisions: interruptible non-blocking play + opt-in emotion overlays.*

- Wire `character.prompts` overrides into `SpeakAction` / `ResponseGenerator` (CONFIG was ahead of code)
- Include formatted `recent_dialogue` in response prompt vars; Lumen-specific `characters/lumen/prompts/*.md`
- Anti-invention rules must whitelist Known facts / prior-session memory, not only this-turn user text

*Session end (Lumen dialogue) — prompt-override path now live; recent dialogue is first-class in Act/Speak prompts.*

- Lumen response prompt uses hard rules: default no question (~1/3 max), ban coach openers, don't reopen unrelated prior goals; curiosity default 0.55 in pack YAML

*Session end (anti-interview) — still data/prompt-only; no pipeline architecture change.*

- Anti-bland pass stays data/prompt-only: Lumen `response_generator.md` + quirks + `help_when_useful` priority; no pipeline or event-bus changes
- No new architecture this turn beyond continuing the “companion feel via pack prompts” approach

*Session end (anti-bland) — Architectural Decisions unchanged at the system level; character-pack prompt iteration only.*

- Nothing changed this turn — no code/architecture edits; live retest only. Prior pack-prompt approach stands; treat anti-bland Lumen prompts as accepted for the upcoming commit

*Session end (anti-bland retest) — Architectural Decisions unchanged.*

*Session end (commit) — Architectural Decisions unchanged; prior local polish decisions now on origin as `0ad03d7`.*

*Session end — nothing changed.*

- Nothing changed — status discussion only. Prior locks stand: Phase 1b = TTS output only; Phase 2 = Studio; Phase 3 = STT/vision

*Session end (next-steps) — Architectural Decisions unchanged.*

- Defer Phase 2 until after further live brain testing via agent-driven text probes
- Probe loop stays data/prompt-driven (pack YAML/prompts); no new architecture proposed this turn — use existing session/CLI flags

*Session end (probe plan) — Phase 2 start postponed; probe approach locked as text-only + memory reset + inspect/refine.*

- Use ephemeral Lumen sessions (`persist=False`) + gitignored `data/probe/lumen_agent_transcript.json` for agent-driven evaluation (no product architecture change)
- No pack/prompt edits this turn — observe first, then decide tweak vs continue

*Session end (lumen probe) — Architectural Decisions unchanged at system level; probe workflow only.*

- Keep Phase 1a foundation hardening in prompts + relationship-stance copy + memory canonicalization (no pipeline redesign)
- Treat Lumen as the Phase 1a reference character for “met a companion, not a chatbot” evaluation; Redbeard remains hard-mode / pirate text

*Session end (foundation probe) — small data/prompt/memory fixes only; event-bus lifecycle unchanged.*

*Session end — Architectural Decisions unchanged this close-out.*

- Nothing new at system level this turn — committed prior foundation decisions as `18eb8ba`
- Defer Phase 2 until Redbeard agent text probe completes the two-sided character spectrum check

*Session end (commit) — Architectural Decisions unchanged beyond recording `18eb8ba` + Redbeard-before-Phase-2 gate.*

- Mirror Lumen: Redbeard gets pack prompt overrides (`characters/captain-redbeard/prompts/*`) rather than changing global prompts for all characters
- Keep stranger suspicion in emotional stance; constrain spoken quiz rate in character prompts + goal copy

*Session end (redbeard probe) — still data/prompt-driven; no pipeline change.*

*Session end — Architectural Decisions unchanged this close-out.*

*Session end (commit) — Architectural Decisions unchanged; prior Redbeard pack-prompt decisions now on origin as `f64e4c8`.*

- Nothing changed — status discussion only. Prior locks stand: Phase 1a/1b complete for demos; Phase 2 = Studio; Phase 3 = STT/vision

*Session end (next-steps) — Architectural Decisions unchanged.*

- **Accept Phase 1a** (text brain + spectrum validation) and start Phase 2
- Phase 2 MVP = CLI Character Studio (`character-os-studio` + `studio/StudioService`) before any GUI
- Studio reads/writes packs via `loader/` paths and inspects SQLite via `persistence/`; chat remains `CharacterSession` / event bus only

*Session end (phase-2 kickoff) — CLI-first Studio locked; web UI deferred.*

*Session end — Architectural Decisions unchanged this close-out.*

*Session end (commit) — Architectural Decisions unchanged; prior CLI-first Studio decisions now on origin as `cdd474c`.*

- Nothing changed — status discussion only. Prior lock stands: CLI Studio before GUI; chat stays on the event bus

*Session end (next-steps) — Architectural Decisions unchanged.*

- Add Studio `validate` + `set` on `StudioService` (still no chat bus involvement)
- Character edits: mutate YAML dict → write with `.bak` → `validate_character_pack` / `load_character` → roll back on failure

*Session end (studio edit) — edit/validate stays in studio/; data-driven packs remain source of truth.*

*Session end — Architectural Decisions unchanged this close-out.*

*Session end (commit) — Architectural Decisions unchanged; prior validate/set decisions now on origin as `e13f7b5`.*

- Nothing changed — status briefing only. Prior locks stand.

*Session end (status) — Architectural Decisions unchanged.*

- Finish Phase 2 Studio on the existing service layer: world editors + richer character list edits + `trace` + optional `serve` (stdlib HTTP), still no chat bus bypass
- Web UI is inspect/validate/trace only for MVP — pack mutation remains CLI-first (`set` / `create-*` / `add-knowledge`)
- Stage traces use `EventBus.start_recording()` so lifecycle order matches Observe → … → Act

*Session end (phase-2 expansion) — Phase 2 Studio surface locked as CLI + thin optional web.*

*Session end (journal close) — Architectural Decisions unchanged this turn.*

*Session end (UI accept) — Architectural Decisions unchanged.*

*Session end (session close) — Architectural Decisions unchanged.*

## Problems

- ~~Trailing session-end edits in `2026-07-16.md` uncommitted~~ — committed/pushed (`3b3edef`)
- ~~Live OpenAI TTS not yet human-verified in this session (stub path verified)~~ — verified by user (play + saved mp3)
- `--tts` alone with pack provider `openai` still needs `OPENAI_API_KEY` (expected)
- This session-end journal append may be uncommitted until the next commit
- ~~Live OpenAI TTS + play: user instructed to test; result not yet reported back~~ — reported: works; voice flat
- Redbeard spoken delivery is monotone / robotic despite pirate text and existing `instructions`

*Session end (live verify) — pipeline OK; open problem is pirate expressiveness / anti-monotone tuning.*

- ~~Redbeard spoken delivery is monotone / robotic~~ — mitigation attempted (`cedar` + stronger instructions); user retest pending
- Voice-tune edits (`character.yaml`, `tests/voice/test_tts.py`, journal) may be uncommitted until next commit

*Session end (voice tune) — open: confirm whether cedar sounds less flat; iterate voice/instructions if needed.*

- ~~confirm whether cedar sounds less flat~~ — confirmed: different timbre, still monotone
- Open: apply simplified (non-accent) Redbeard vocal brief, or approve Plan B new character

*Session end (cedar retest) — monotone unresolved; strategy chosen but not applied yet.*

- ~~Open: apply simplified (non-accent) Redbeard vocal brief~~ — applied; listen retest pending
- Voice-tune / simplify-brief edits still uncommitted

*Session end (simplify brief) — open: does the non-dialect brief fix monotone? Plan B new character if not.*

- ~~does the non-dialect brief fix monotone?~~ — yes for robot/flat, but voice lost character (generic man)
- Open: does `ash` + grit/swagger brief restore character without dialect? Listen retest pending
- TTS profile edits still uncommitted

*Session end (character color) — open problem is characterful-but-natural delivery, not pipeline.*

- ~~does `ash` + grit restore character without dialect?~~ — partial: better prosody, but still “bad pirate impression”
- Mitigation applied: speech-only orthography normalize + anti-impression instructions; listen retest pending
- Phase 1b TTS polish still uncommitted (`speech_text.py`, profile fields, Redbeard YAML, docs)

*Session end (anti-impression) — open: does normalize+anti-impression kill the failed pirate act?*

- Redbeard voice character still unresolved (normalize retest may be superseded by Plan B focus)
- ~~Lumen OpenAI dialogue + TTS not yet human-verified~~ — user tried `sage`, disliked it; cedar retune pending confirm
- Large uncommitted Phase 1b stack: Redbeard TTS tunes, `speech_text.py`, Lumen pack, `everyday-present` world, docs/tests

*Session end (Lumen pack) — open: live Lumen chat+`--tts-play` feel-check.*

- Open: does `cedar` @ 0.88 match the calm deliberate companion vibe?
- Phase 1b stack still uncommitted

*Session end (Lumen TTS retune) — open: cedar retest only.*

- ~~does `cedar` @ 0.88 match the calm deliberate companion vibe?~~ — accepted as good-enough robot companion
- **Blocking:** Phase 1b stack still uncommitted/unpushed on `feature/dev`
- Redbeard spoken character remains imperfect (non-blocking)

*Session end (status) — only open blocker is commit/push; voice quality no longer blocking 1b MVP.*

- ~~**Blocking:** Phase 1b stack still uncommitted/unpushed on `feature/dev`~~ — pushed (`ee7f10f`)
- Redbeard spoken character remains imperfect (non-blocking)
- This session-end journal append may be uncommitted until a follow-up commit

*Session end (commit) — no Phase 1b blockers; optional polish / Phase 2 remain choices.*

- Trailing `2026-07-17.md` session-end notes still uncommitted after `ee7f10f`
- User has not yet chosen Phase 2 vs further TTS polish

*Session end (next-steps) — only open items are journal housekeeping + direction choice.*

- ~~User has not yet chosen Phase 2 vs further TTS polish~~ — chose voice polish
- Voice polish stack uncommitted (emotion overlay, playback, Lumen tune, tests, journal)
- Live retest of polish not yet reported
- Redbeard spoken character remains imperfect (non-blocking)

*Session end (voice polish) — open: user listen confirm + commit polish when ready.*

- ~~Misread Lumen wildfire opener as hallucination~~ — corrected: prior-session memory
- Mid-session coherence + coach-loop mitigations applied; live retest of dialogue fixes still pending
- Combined uncommitted stack: voice polish + Lumen dialogue/prompt fixes + journal

*Session end (Lumen dialogue) — open: user retest coherence, then commit the polish+dialogue stack.*

- ~~coherence-only retest~~ — second live chat showed interview loop still dominant; stronger anti-interview prompts applied, retest again
- Persisted Lumen curiosity may remain high from older SQLite rows despite YAML 0.55
- Large uncommitted stack: voice polish + dialogue/coherence + anti-interview + journal

*Session end (anti-interview) — open: retest question rate / openers; then commit or keep iterating.*

- ~~anti-interview retest~~ — questions largely gone; still bland/assistant-y; anti-bland prompt pass applied
- ~~Anti-bland live retest (concrete naming + non-assistant goal answers) still pending~~ — accepted after restart: understand/remember + concrete recall; mild soft closer leftover non-blocking
- Persisted Lumen curiosity may remain high from older SQLite rows despite YAML 0.55 (non-blocking if behavior is acceptable)
- Large uncommitted stack: voice polish + dialogue/coherence + anti-interview + anti-bland + journal — **ready to commit**

*Session end (anti-bland retest) — open: commit+push the polish+dialogue stack when user asks.*

- ~~Large uncommitted polish+dialogue stack~~ — committed/pushed as `0ad03d7`
- Mild Lumen soft closers / repeated-fact listing remain optional later (non-blocking)
- Persisted Lumen curiosity may still differ from YAML 0.55 until reset (non-blocking)
- This journal session-end append may be uncommitted until the next commit

*Session end (commit) — open: choose Phase 2 vs optional streaming TTS next.*

*Session end — no new blockers; only optional leftovers + this journal append dirty.*

- No Phase 1b blockers remain
- User has not yet chosen Phase 2 vs optional 1b leftovers (streaming TTS / mild Lumen tone / curiosity reset)
- Trailing journal appends (including this next-steps close) may be uncommitted until the next commit

*Session end (next-steps) — open: user chooses Phase 2 start vs optional polish.*

- ~~User has not yet chosen Phase 2 vs optional 1b leftovers~~ — chose further testing before Phase 2 (agent text probe)
- First agent-driven Lumen (or Redbeard) probe script/run not yet executed
- No dedicated “wipe all memories” CLI command yet — reset is `--no-persist` or manual SQLite delete
- Trailing journal appends may be uncommitted until the next commit

*Session end (probe plan) — open: run first scripted text-only probe when user confirms.*

- ~~First agent-driven Lumen probe not yet executed~~ — 3-turn evaluator probe done; transcript in `data/probe/`
- Lumen internal thoughts invent body language / posture from text-only input
- Soft question still appears on meta/evaluator openers (anti-interview not 100%)
- Tone can still read slightly analytical / chatbot-warm on early turns
- No dedicated `/reset` CLI (using `--no-persist` + transcript replay)
- Trailing journal appends may be uncommitted until the next commit

*Session end (lumen probe) — open: continue probe conversation or fix thought-channel + opener question first.*

- ~~Lumen thoughts invent body language~~ — fixed in thought prompts; 0/8 on retest
- ~~Soft question on meta/evaluator openers~~ — fixed; 0/8 questions on foundation probe
- ~~`dog's name is X` corrupted to user's name on dedupe/load~~ — fixed `_NAME_RE` + test
- World-pack knowledge still weakly surfaced in spoken replies (companion-role answer was generic)
- Occasional soft closers remain (non-blocking)
- Foundation tweak stack (prompts, emotion stance, memory regex, journal) may be uncommitted

*Session end (foundation probe) — open: user confirm Phase 1a accept → commit foundation fixes → then Phase 2 or more probes.*

*Session end — no new problems; foundation fixes + journal still uncommitted until user asks.*

- ~~Foundation tweak stack uncommitted~~ — committed/pushed as `18eb8ba`
- Redbeard agent text probe not yet run (spectrum validation still open)
- World-pack knowledge still weakly surfaced for Lumen (non-blocking)
- Occasional soft closers remain (non-blocking)
- This journal session-end append may be uncommitted until the next commit

*Session end (commit) — open: run Redbeard text probe; then Phase 1a accept / Phase 2.*

- ~~Redbeard agent text probe not yet run~~ — done; anti-interview harden applied locally
- Occasional rhetorical `eh?`/`savvy?` and rare content questions remain (acceptable for pirate flavor)
- Redbeard prompt overrides + goal tweak + journal still uncommitted
- World-pack knowledge still weakly surfaced for Lumen (non-blocking)

*Session end (redbeard probe) — open: commit Redbeard harden when user asks; then Phase 1a accept across both / Phase 2.*

*Session end — no new problems; Redbeard harden + journal still uncommitted.*

- ~~Redbeard prompt overrides + goal tweak uncommitted~~ — committed/pushed as `f64e4c8`
- Occasional rhetorical `eh?`/`savvy?` and rare content questions remain (acceptable for pirate flavor)
- World-pack knowledge still weakly surfaced for Lumen (non-blocking)
- This journal session-end append may be uncommitted until the next commit

*Session end (commit) — open: user Phase 1a accept across both characters; then Phase 2.*

- No Phase 1a/1b blockers remain
- User has not yet formally accepted Phase 1a / started Phase 2
- Trailing journal appends (including this next-steps close) may be uncommitted vs `f64e4c8`
- Optional leftovers only: Lumen world-knowledge soft, streaming TTS, `/reset` CLI, mild rhetorical pirate tags

*Session end (next-steps) — open: Phase 1a accept → Phase 2, or optional polish.*

- ~~User has not yet formally accepted Phase 1a / started Phase 2~~ — accepted by agent choice; Studio CLI MVP started
- Studio does not yet edit personalities/worlds in-place (scaffold + inspect only)
- Phase 2 stack local/uncommitted until user asks to commit
- Optional leftovers remain: Lumen world-knowledge soft, streaming TTS, `/reset` CLI

*Session end (phase-2 kickoff) — open: commit Studio MVP; next slices are edit/validate + richer debug.*

*Session end — no new problems; Studio MVP + journal still uncommitted.*

- ~~Phase 2 Studio CLI MVP uncommitted~~ — committed/pushed as `cdd474c`
- Studio still scaffold + inspect only (no in-place personality/world editors yet)
- Optional leftovers remain: Lumen world-knowledge soft, streaming TTS, `/reset` CLI
- This journal session-end append may be uncommitted until the next commit

*Session end (commit) — open: next Studio slices (edit/validate, stage-debug) or optional polish.*

- No blockers on shipped Studio CLI MVP
- In-place personality/world editors + validation still missing (recommended next)
- Stage-debug viewer not started
- Trailing journal appends may be uncommitted vs `cdd474c`
- Optional leftovers unchanged (streaming TTS, `/reset`, Lumen world-knowledge)

*Session end (next-steps) — open: user starts edit/validate slice or chooses alternate.*

- ~~In-place personality/world editors + validation still missing~~ — character validate/set shipped locally; world edit still missing
- `set` rewrites YAML (loses comments/formatting) — documented
- Stage-debug viewer not started
- Studio edit/validate stack local/uncommitted
- Optional leftovers unchanged (streaming TTS, `/reset`, Lumen world-knowledge)

*Session end (studio edit) — open: commit edit/validate; next could be world edit or stage-debug.*

*Session end — no new problems; edit/validate + journal still uncommitted.*

- ~~Studio edit/validate stack uncommitted~~ — committed/pushed as `e13f7b5`
- World pack scaffold/edit still missing
- `set` rewrites YAML (loses comments/formatting) — documented, non-blocking
- Stage-debug viewer not started
- This journal session-end append may be uncommitted until the next commit
- Optional leftovers unchanged (streaming TTS, `/reset`, Lumen world-knowledge)

*Session end (commit) — open: world edit or stage-debug next.*

- No new blockers from the status review
- Phase 2 incomplete: world scaffold/edit, stage-debug, richer editors, GUI
- Trailing journal appends (including this status close) may be uncommitted vs `e13f7b5`
- Optional leftovers unchanged

*Session end (status) — open: choose world edit vs stage-debug (or polish).*

- ~~Phase 2 incomplete: world scaffold/edit, stage-debug, richer editors, GUI~~ — implemented locally (world + richer set + `trace` + `serve`)
- `trace` uses repo character packs via `CharacterSession` (alternate `--root` scaffolds are not loaded into the live pipeline)
- Phase 2 expansion stack local/uncommitted until user asks to commit
- Optional leftovers unchanged (streaming TTS, `/reset`, Lumen world-knowledge)

*Session end (phase-2 expansion) — open: commit Phase 2 expansion; then Phase 3 or polish.*

- Open items unchanged from Phase 2 expansion close: expansion stack still local/uncommitted; `trace` still loads packs via repo `CharacterSession` (not alternate `--root`); optional leftovers (streaming TTS, `/reset`, Lumen world-knowledge) unchanged
- This journal close-out itself may remain dirty until the next commit

*Session end (journal close) — no new problems; same open commit + optional leftovers.*

- ~~Optional web UI unverified~~ — user accepted (`serve` looks good)
- Phase 2 expansion stack still local/uncommitted
- Optional leftovers unchanged (streaming TTS, `/reset`, Lumen world-knowledge)

*Session end (UI accept) — open: commit Phase 2 expansion when ready.*

*Session end (session close) — open items unchanged: commit Phase 2 expansion; optional leftovers remain non-blocking.*

## Future Ideas

### Immediate
- ~~Phase 1a spectrum probes + hardens~~ — done (`18eb8ba`, `f64e4c8`)
- ~~Start Phase 2 Character Studio~~ — done
- ~~Commit Phase 2 Studio CLI MVP~~ — done (`cdd474c`)
- ~~Studio edit + YAML validation~~ — done (`e13f7b5`)
- ~~World pack scaffold/edit, richer editors, stage-debug, optional web UI~~ — done locally (commit when asked)
- Optional: streaming TTS, `/reset` CLI, stronger Lumen world-knowledge pull
- Optional: commit trailing journal-only appends
- Optional: web UI pack mutation forms; `trace` honor Studio `--root`

### Phase 1b optional polish (remaining)
- True streaming / lower-latency synthesis (still open, non-blocking)
- Further Redbeard TTS polish (hard mode)

### Next major phase
- **Phase 2** — Character Studio — authoring/debug goals complete locally; ship then move to Phase 3

### Later (unchanged)
- Speech recognition / vision (Phase 3)
- Embodiment (Phase 4)
- Interactive experiences (Phase 5)

*Session end (phase-2 expansion) — next: commit, then Phase 3 or optional polish.*

*Session end (journal close) — Future Ideas unchanged: commit Phase 2 expansion, then Phase 3 or optional polish.*

*Session end (UI accept) — next: commit Phase 2 expansion (world/richer-edit/trace/serve), then Phase 3 or polish.*

*Session end (session close) — Future Ideas unchanged: commit Phase 2 expansion next, then Phase 3 or optional polish.*
