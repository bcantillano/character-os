# 2026-07-17

## Today's Goal

- New-day status: Phase 1a complete — confirm Phase 1b TTS as next major workstream

**Outcome:** Confirmed. Phase 1a is closed on `feature/dev` (`8f02645`). Next major workstream is **Phase 1b — voice output (TTS)**. No implementation started this session.

*Session end — status check only; no further goals pursued.*

## What We Learned

- Roadmap / structure already lock Phase 1b scope: TTS + character voice profiles; STT deferred to Phase 3
- Docs already point at the integration shape: `behavior/actions/speak_voice.py` (or `voice/`) consuming `ResponseReadyEvent` without rewriting the text pipeline

*Session end — no additional learnings beyond the status check.*

## Architectural Decisions

- No new decisions this session
- Prior lock stands: Phase 1b is TTS output only; speech recognition stays Phase 3; ticks remain state-only (no unprompted speech)

*Session end — nothing changed in Architecture.*

## Problems

- Trailing session-end edits in `2026-07-16.md` still uncommitted (and `2026-07-17.md` is new/untracked) — housekeeping commit still pending
- No Phase 1b blockers identified yet (provider, voice YAML schema, CLI flag not scoped)

*Session end — only open item is journal housekeeping; Phase 1b not started.*

## Future Ideas

### Next (Phase 1b)
- Scope TTS provider choice, voice profile YAML shape, and CLI flag (e.g. `--voice`)
- Wire speak-voice on `ResponseReadyEvent` beside existing text Act path

### Later (unchanged from 2026-07-16)
- Optional per-character relationship scoring overlays in YAML
- Brain module split
- Richer archive UI if needed beyond `/archive` `/restore`

*Session end — Future Ideas narrowed to Phase 1b kickoff; later items unchanged.*
