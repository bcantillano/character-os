# 2026-07-26

## Today's Goal

- Commit + push the leftover `2026-07-22` journal session-end note so origin matches local
- Clarify project next steps for the user
- Start Phase 3 World Awareness — STT MVP
- Unblock `/listen` mic capture (install ffmpeg)
- Live-verify OpenAI STT + TTS with Lumen
- Tune Lumen companion tone after user reported stenographer replies

**Outcome (commit/push):** Pushed `407f93f` — journal note that portfolio mobile polish + scroll-on-refresh shipped as `a648273` — to `origin/feature/dev`.

**Outcome (next steps):** Restated roadmap: Phases 1–2 + portfolio Pages polish on `feature/dev`. Recommended next was Phase 3 STT.

**Outcome (mobile verify):** User confirmed live Pages mobile layout looks better.

**Outcome (Phase 3 STT MVP):** Added `awareness/` (STT provider stub + OpenAI Whisper, `SpeechInputBridge`, ffmpeg push-to-talk). `SpeechRecognizedEvent` → `UserMessageEvent` → existing pipeline. CLI: `--stt`, `--stt-file`, `/listen`. Docs: `PHASE_3_AWARENESS.md`, ROADMAP/README/.env.example. Tests under `tests/awareness/` + integration (9 related passing). Stub `--stt-file` CLI smoke worked locally.

**Outcome (ffmpeg):** User hit `/listen` → `ffmpeg not found on PATH`. Installed via Homebrew (`ffmpeg` 8.1.2 at `/opt/homebrew/bin/ffmpeg`). Character OS `ffmpeg_available()` confirms True.

**Outcome (live STT+TTS smoke):** User ran Lumen with `--provider openai --stt openai --tts-play`. Three `/listen` turns transcribed accurately (greeting; Nolan / The Odyssey; IMAX). Later turns remembered film plans. First reply misfired as a first-meeting intro (“I'm Lumen…”) with thoughts referencing a “long day” not said that turn — companion-tone / memory issue, not STT failure.

**Outcome (Lumen tone probe + retune):** Baseline OpenAI probe as Byron (Odyssey → Ira/Z at AMC Tysons → IMAX → pretzel burger) matched user complaint: “noted” + soft closers. Root cause: earlier anti-interview prompts taught stenographer replies. Retuned `response_generator.md`, `internal_thoughts.md`, `character.yaml`.

**Outcome (Lumen tone continue):** Second pass — mandatory reply shape (concrete detail → warm reaction → optional follow-up), final-check rewrite on soft fillers, question gate allows concrete follow-ups on newly named people/places/food, hard-bans abstract film questions. Double OpenAI probe clean (no soft-filler / abstract hits): who? → food? → IMAX statement with friends → pretzel joke. Loader test markers updated.

*Session end (commit/push) — STT live + Lumen tone shipped to origin.*

## What We Learned

- `SpeechRecognizedEvent` was already stubbed in `events/types.py` but had no publishers/subscribers
- PHASE_1_STRUCTURE said STT might publish `UserMessageEvent` directly; lifecycle/journal prefer an explicit speech Observe event plus bridge (structure doc updated to match)
- Mic capture can stay dependency-light via `ffmpeg` (mirrors TTS system-player approach); stub STT needs no mic
- Stub LLM replies are character-agnostic (Lumen `--stt-file` smoke still sounded pirate) — pre-existing, not STT-specific
- `/listen` hard-depends on system `ffmpeg`; Homebrew on Apple Silicon puts it at `/opt/homebrew/bin/ffmpeg`
- Live Whisper transcripts were clean enough for multi-turn memory (Nolan → Odyssey → IMAX)
- Over-correcting interviewer bans into “statements only / X — noted” makes Lumen feel uninterested
- Soft fillers need reply-shape + final-check rewrite, not ban lists alone
- Rigid alternate-turn question bans fight natural chat when the user keeps adding new named details; better gate = concrete follow-up on new detail, statements on mood/IMAX weave, never abstract essay questions

*Session end — stenographer↔conversation balance and concrete-vs-abstract question gate are the main tone lessons.*

## Architectural Decisions

- Always wire `SpeechInputBridge` so speech Observe joins the shared interpret path without changing `ConversationInterpreter`
- Mirror TTS: `STTProvider` ABC + stub/openai factory; vendor SDK stays in `awareness/providers/`
- Stub `/listen` skips mic and uses the default transcript (CI-friendly)
- Defer continuous listening, VAD, vision, and local Whisper to later Phase 3 slices
- Keep mic capture on host `ffmpeg` (no new Python audio deps this slice)
- Lumen tone stays data-driven (prompt/YAML packs only) — no brain code changes for companion curiosity retune
- Question gate: at most one `?` per reply; concrete follow-ups OK on newly named people/places/food; block abstract film/feeling questions; mood+IMAX after a rolling plan → statements that weave friends/place

*Session end — Architectural Decisions updated only for the refined Lumen question gate; STT architecture unchanged.*

## Problems

- ~~ffmpeg missing on PATH~~ — installed via Homebrew (8.1.2)
- ~~OpenAI Whisper + `/listen` unverified~~ — live smoke succeeded
- ~~Stenographer “noted” companion tone~~ — retuned; agent double-probe clean on Odyssey/AMC script
- Soft fillers / abstract questions may still rare-leak under other scripts (not seen on last two clean probes)
- First-meeting intro misfire on casual greeting still possible with stale/low familiarity memory
- Thoughts still sometimes plan with banned words even when spoken reply is clean
- ~~Phase 3 STT MVP + Lumen tone + this journal local/uncommitted~~ — shipping this commit
- ~~`test_lumen_prompt_override_loads` stale markers~~ — updated for reply-shape / question-gate / final-check

*Session end (commit/push) — STT + Lumen tone on origin; optional user retest remains.*

## Future Ideas

### Immediate
- User retests Odyssey / Ira / Z / AMC thread (typed or `/listen`) with retuned Lumen
- ~~Commit + push Phase 3 STT MVP + Lumen tone~~ — done this session
- Document `brew install ffmpeg` in PHASE_3 / README prerequisites
- Then Phase 3 vision / context when STT + tone feel solid

### Later
- Optional code-side tone guard if soft fillers return under load
- Continuous listening / VAD
- Local offline Whisper
- Phase 4 embodiment; Phase 5 multi-character experiences
- Optional portfolio hamburger nav / custom domain

*Session end (commit/push) — next is optional live retest, then Phase 3 vision when ready.*

