# 2026-07-18

## Today's Goal

- New-day status: where we stand after Phase 2 ship + local streaming TTS work

**Outcome (status):** On origin through `01fed02`: Phase 1a, 1b MVP, Phase 2 Studio complete. Local/uncommitted: sentence-chunked low-latency TTS (`--tts-play`). Recommended next after commit: Phase 3 STT, or finish committing the TTS polish first.

*Session end (status) — roadmap restated; awaiting commit TTS vs Phase 3 choice.*

*Session end — journal close-out; no further goals this turn.*

- Live-test low-latency TTS: start a Lumen conversation and observe chunked play

**Outcome (TTS probe):** Ran 2-turn OpenAI Lumen session (`persist=False`, TTS play on). Both turns produced **3 speak-chunks**; first chunk ready ~1.5–2.0s after TTS start vs ~4.6–4.9s sum of all chunk synth; `audio_path` = first `.0.mp3`. Multi-chunk path confirmed. CLI/`send_message` still blocks until all chunks synthesize (sync handler) — ear latency wins; prompt return does not. Stack still local/uncommitted.

*Session end (TTS probe) — live chunked TTS verified; commit when user asks.*

*Session end — journal close-out; no further goals this turn.*

- Give user local CLI commands to test low-latency TTS themselves

**Outcome (TTS test commands):** Provided activate + `character-os --character lumen --provider openai --tts --tts-play --show-thoughts` (and `--once` smoke). No code changes. Awaiting user listen results; stack still uncommitted.

*Session end (TTS test commands) — guidance only; human verify pending.*

*Session end — journal close-out; no further goals this turn.*

- Debrief user live Lumen chat after low-latency TTS changes

**Outcome (user TTS chat):** User ran multi-turn lunch chat with `--tts-play --show-thoughts`. Disk confirmed chunking: 3+2+4+2 mp3 parts across turns; CLI correctly prints only `.0.mp3`. TTS path accepted as working. Dialogue issues separate: thoughts stuck on stale prefs (burger/Cajun/air quality/healthy eating); flatbread correction under-used in later recs; turn-4 reply analytical (“I observe the patterns…”). Awaiting user choice: commit TTS as-is vs fix memory/coherence first.

*Session end (user TTS chat) — chunked TTS human-verified via files + chat; companion coherence gaps noted.*

*Session end — journal close-out; no further goals this turn.*

## What We Learned

*Session start (status) — no new learnings yet.*

*Session end (status) — nothing new; confirmed Phase 1–2 on origin and streaming TTS still local-only.*

*Session end — nothing further learned this close-out.*

- Live probe: sentence chunking engages on multi-sentence Lumen replies; playback can start after chunk 0 while later chunks still synthesize
- `SpeakVoiceAction` remains synchronous, so end-to-end `send_message` time still includes all chunk synths — perceived latency ≠ CLI unblock latency

*Session end (TTS probe) — chunked-play latency win confirmed; sync-handler caveat noted.*

*Session end — nothing further learned this close-out.*

*Session end (TTS test commands) — nothing new; restated existing CLI flags only.*

*Session end — nothing further learned this close-out.*

- User session audio dir showed `.0`–`.N` chunk files while CLI only surfaces `.0` — expected; multi-chunk play is invisible in the `[tts]` line
- Prior-session food prefs are fine for “guess what I’m eating,” but after an explicit correction (flatbread) thoughts/replies must prefer the new fact over looping burger/Cajun/healthy-eating memory
- Analytical closers (“I observe the patterns…”) still sneak through on meta compliments

*Session end (user TTS chat) — TTS chunking confirmed on user machine; Lumen recent-fact vs stale-pref drift is the open dialogue learning.*

*Session end — nothing further learned this close-out.*

## Architectural Decisions

*Session start (status) — Architectural Decisions unchanged; prior locks stand.*

*Session end (status) — Architectural Decisions unchanged.*

*Session end — Architectural Decisions unchanged this close-out.*

*Session end (TTS probe) — Architectural Decisions unchanged; prior chunked `--tts-play` design stands (no background synth-after-return change this turn).*

*Session end — Architectural Decisions unchanged this close-out.*

*Session end (TTS test commands) — Architectural Decisions unchanged.*

*Session end — Architectural Decisions unchanged this close-out.*

*Session end (user TTS chat) — Architectural Decisions unchanged (no code); fork is commit TTS vs dialogue/memory prompt pass.*

*Session end — Architectural Decisions unchanged this close-out.*

## Problems

- Streaming/low-latency TTS implemented locally but **not committed/pushed** vs `01fed02`
- Trailing `2026-07-17.md` journal appends also dirty with that stack
- Optional leftovers remain: `/reset`, Lumen world-knowledge, Studio polish, PCM/LLM streaming TTS
- User has not yet chosen: commit streaming TTS first vs start Phase 3
- This journal session-end append may remain dirty until the next commit

*Session end (status) — open: commit TTS stack or start Phase 3.*

*Session end — open items unchanged.*

- Live TTS probe passed functionally; human speaker listen not independently confirmed from agent side
- Sync handler still blocks CLI until all chunks done (optional follow-up: synthesize remaining chunks in background after first enqueue)
- Streaming TTS + journal still uncommitted

*Session end (TTS probe) — open: commit verified TTS stack; optional background-synth polish later.*

*Session end — open items: commit TTS; then Phase 3 or leftovers.*

- User given local test commands; human listen result not yet reported
- Streaming TTS + journals still uncommitted vs `01fed02`
- Open items otherwise unchanged

*Session end (TTS test commands) — open: user listen confirm; then commit or keep iterating.*

*Session end — open items unchanged (commit TTS; Phase 3; journal dirty).*

- ~~human listen / local TTS verify pending~~ — user chat + chunk files on disk confirm low-latency path
- Lumen coherence: stale long-term food prefs dominate thoughts after correction; recommendations ignored flatbread; mild analytical tone on meta turns
- Streaming TTS stack still uncommitted
- User choice pending: commit TTS first vs fix memory/coherence first

*Session end (user TTS chat) — open: commit vs coherence fix; TTS functionally accepted.*

*Session end — open: commit TTS or fix Lumen recent-fact drift; journal dirty.*

## Future Ideas

### Immediate
- Commit + push streaming TTS stack (user-verified via chunk files + live chat)
- Optional: Lumen coherence pass — prefer recent corrections over stale food prefs; less analytical meta replies
- Then **Phase 3** (speech recognition) or remaining optional polish
- Optional: background remaining-chunk synth so CLI returns after first audio

### Later
- Phase 3 — World Awareness (STT, vision, context)
- Phase 4 — Embodiment
- Phase 5 — Interactive experiences
- Optional: PCM/`ffplay` byte streaming; LLM token→TTS pipeline

*Session end (status) — Future Ideas unchanged: commit TTS, then Phase 3 or polish.*

*Session end — Future Ideas unchanged.*

*Session end (TTS probe) — next: commit streaming TTS; optional CLI-unblock polish; then Phase 3.*

*Session end — Future Ideas unchanged beyond noting live verify done.*

*Session end (TTS test commands) — Future Ideas unchanged: await user listen, then commit TTS → Phase 3 or polish.*

*Session end — Future Ideas unchanged.*

*Session end (user TTS chat) — next: user picks commit TTS vs Lumen coherence fix.*

*Session end — Future Ideas: await commit-vs-coherence choice.*
