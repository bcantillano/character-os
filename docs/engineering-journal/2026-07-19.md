# 2026-07-19

## Today's Goal

- Solidify Phase 1–2 via optional leftovers before Phase 3

**Outcome (leftovers):** Implemented the leftover stack locally (82 tests green). Shipped in order:
1. Chat `/reset confirm` + `CharacterPersistence.reset_character` / `CharacterSession.reset`
2. Lumen world-knowledge: `shared_places.yaml` (quiet corner / kitchen table / late-night desk) first in everyday-present index + response prompt grounding rule/example
3. Food preference fact merge (`pref:food` soft-key + merge tails; tea stays separate)
4. Background remaining-chunk TTS synth so `--tts-play` unblocks after first audio
5. Studio `reset-memories <id> --confirm` + docs

Awaiting commit when user asks. Phase 3 still deferred until user accepts Phase 1–2 as solid.

*Session end (leftovers) — Phase 1–2 leftover polish complete locally (82 tests); commit + optional live probe pending; Phase 3 deferred.*

- Live probe after `/reset confirm` (Lumen OpenAI + TTS)

**Outcome (reset probe):** `/reset` wiped 9 memories correctly; name unknown → Byron noted → self-ID as companion worked. Leaks: "clear" treated as philosophy; "remember me" invented recognition from wording; "I don't have a personal name"; soft connection-coaching in thoughts. Tightened Lumen thought + response prompts with examples. Awaiting retest (`/reset confirm` then same beats).

*Session end (reset probe) — reset accepted; tone micro-pass local; retest pending before commit.*

- Retest after reset: greeting too curt; want intro + measured curiosity

**Outcome (companion balance):** User retest: `/reset` OK; "I'm Lumen" OK; "Byron — noted" OK; but first "Hello — noted" too short, and curiosity under-fired (wanted day/mind prompts without endless probing). Rebalanced Lumen pack: first-meeting self-intro, ~one light question every few early turns, stop probing once they're sharing. Awaiting retest.

*Session end (companion balance) — warm intro + bounded curiosity applied; retest pending before commit.*

- Status check: what are our next steps for character-os?

**Outcome (status):** Phases 1a–2 + Lumen refine shipped on `origin/feature/dev` through `8210716`. Recommended next major workstream: **Phase 3 World Awareness — STT first**. Optional leftovers remain non-blocking (`/reset`, Lumen world-knowledge, Studio polish, background chunk synth, preference-fact merge). User choice pending. No code changes this turn.

*Session end (status) — roadmap fork restated; awaiting Phase 3 vs leftovers.*

- Commit + push memory/coherence fix and pending TTS commit; confirm next roadmap step

**Outcome (commit/push):** Pushed TTS `b8a9236` + Lumen recent-fact coherence fix (`d5cd577`) to `origin/feature/dev`. Working tree clean. Recommended next: Phase 3 STT (or optional flatbread retest / leftovers first).

*Session end (commit/push) — both commits on origin; roadmap fork stated.*

- Refine Lumen after post-fix food chat (health-goal framing)

**Outcome (food refine):** User retest showed flatbread recall working, but thoughts/replies still coach around healthy eating / protein goals. Tightened Lumen response + thought prompts and a voice quirk: food lists stay plain (no diet-coach justification). Awaiting retest.

*Session end (food refine) — anti-diet-coach pass applied locally; retest pending.*

- Agent probe + refine Lumen (fresh memory): recollection, tone, anti-interview

**Outcome (agent refine):** Fresh `persist=False` OpenAI probes (v1–v3). Diet-coach mostly gone after earlier pass. Remaining leaks fixed with stronger bans + good/bad examples: no "That sounds like"/"How about", no movie-essay, no question after shared facts, shorter meta. v3: 0/7 bad thoughts; 1/7 bad replies (soft "Would you enjoy…?" — extra ban added). Recollection of name/flatbread/spicy/Dark Knight solid.

*Session end (agent refine) — companion tone tightened.*

- Commit + push Lumen agent refine

**Outcome (commit):** Pushed `8210716` — Lumen refine (examples, anti-interview/anti-essay, food/meta tone) to `origin/feature/dev`. Working tree was clean after push.

*Session end (commit) — refine on origin (`8210716`); Phase 3 next when ready.*

*Session end — journal close-out after refine push; no further goals this turn.*

## What We Learned

- Anti-interview hard rule ("default: no question marks") made fresh-start Lumen too curt ("Hello — noted."); companions need first-meeting self-intro + bounded early curiosity, then stop probing once the person is sharing
- User expectation for early curiosity is light day/mind prompts — not stress/therapy grilling and not stacked questions

*Session end (companion balance) — rebalance away from absolute no-questions toward measured curiosity.*

- After `/reset`, opaque one-word chat ("clear") is still interpreted as deep meaning unless prompts ban life-advice reframes; users may also confuse bare "clear" with a wipe command (only `/reset confirm` wipes)
- "Do you remember me?" with empty Known facts can invent recognition from the question wording — needs explicit memory-honesty + identity examples ("I'm Lumen", not "I have no personal name")
- Fresh-start path itself works: wipe → unknown name → learn Byron → companion self-description

*Session end (reset probe) — reset works; post-wipe companion tone still needs example-driven bans.*

- Session `_context` only injects the first 5 world knowledge summaries — citable everyday-present places must be ordered early in `knowledge_files` or they never reach the prompt
- Preference dedupe was losing sibling tails because `_merge_group_into_keeper` mutated the keeper (a group member) before reading originals — snapshot contents first
- Sync TTS handlers block `send_message` until all chunks finish; returning after first synth + background remaining chunks separates ear latency from CLI unblock

*Session (leftovers) — ordering, merge snapshot, and TTS thread lessons.*

*Session end (leftovers) — What We Learned unchanged beyond the leftover notes above.*

- On `feature/dev` through `8210716`: Phase 1a brain, 1b TTS (incl. streaming), Phase 2 Studio, Lumen memory/coherence + companion tone refine are all on origin; only trailing journal is dirty locally

*Session end (status) — no new learnings; shipped stack and roadmap fork confirmed.*

*Session start (status) — shipped stack confirmed via log.*

*Nothing new this turn beyond shipping prior local work.*

*Session end (commit/push) — nothing further learned.*

- Recent-fact wiring worked (flatbread appears in thoughts and guesses)
- Remaining drift: long-term "healthy eating / protein goals" still moralizes food lists — need explicit anti-diet-coach rules separate from correction-wins rules

*Session end (food refine) — flatbread OK; health-goal coaching is the next companion leak.*

- Hard rules alone were soft-ignored ("That sounds like", film-essay); short Good/Bad examples in the response prompt landed better
- Preference notable_facts can still split ("likes spicy" / "bold food") — interpreter one-fact rule helps only sometimes

*Session end (agent refine) — examples > more abstract rules for Lumen tone.*

*Session end (commit) — no new learnings; shipped refine as `8210716`.*

*Session end — nothing further learned this close-out.*

## Architectural Decisions

- Replaced absolute "no question marks" with measured curiosity: first-meeting intro required; ~one light question every few early turns; no stacked questions; statements only once they're sharing — still pack-prompt/quirks only, no pipeline change

*Session end (companion balance) — bounded curiosity locked in Lumen pack locally.*

- Post-reset tone leaks fixed with pack-prompt examples only (identity, memory honesty, opaque one-word messages, banned connection-coaching thought frames) — no pipeline change

*Session end (reset probe) — still data/prompt-driven companion iteration.*

- `/reset confirm` required (irreversible wipe of one character's memories + archive + drives + relationship + session conversation; other characters untouched)
- Lumen world-knowledge fix stays data/prompt-first (shared places + grounding rule); no relevance-ranking code this pass
- Food prefs share `pref:food` soft-key when taste markers overlap; non-food likes (e.g. tea) keep distinct keys
- `--tts-play` synthesizes chunk 0 sync then remaining chunks on a daemon thread with generation cancel; `--tts` without play stays single-file sync
- Studio `reset-memories --confirm` reuses the same persistence wipe (does not clear an active chat session)

*Session (leftovers) — decisions above locked in local code.*

*Session end (leftovers) — Architectural Decisions unchanged; leftover decisions still stand.*

*Architectural Decisions unchanged this turn (status-only; no new choices).*

*Session end (status) — Architectural Decisions unchanged.*

*Architectural Decisions unchanged this turn.*

*Session end (commit/push) — Architectural Decisions unchanged.*

- Keep food refine data/prompt-only (Lumen pack); no memory-ranking changes

*Session end (food refine) — still pack-prompt iteration for companion tone.*

- Keep Lumen refine as pack prompts + brief examples; no pipeline change this turn

*Session end (agent refine) — still data/prompt-driven companion iteration.*

*Session end (commit) — Architectural Decisions unchanged; prior refine decisions now on origin as `8210716`.*

*Session end — Architectural Decisions unchanged this close-out.*

## Problems

- Leftover stack + Lumen tone passes (reset micro-pass + companion balance) still local/uncommitted
- Retest pending for first hello intro + light curiosity without endless probing
- Studio polish beyond `reset-memories` deferred (delete packs, web mutation forms, `trace --root`)
- User still to confirm Phase 1–2 solid enough for Phase 3 STT

*Session end (companion balance) — retest warm-intro/curiosity beats, then commit.*

*Session start (status) — choice fork unchanged.*

*Session end (commit) — ~~Lumen refine uncommitted~~ pushed (`8210716`); open: Phase 3 vs leftovers.*

*Session end — open: Phase 3 STT recommended; optional leftovers; trailing journal dirty.*

## Future Ideas

### Immediate
- ~~Commit Lumen agent-refine prompt pass~~ — done (`8210716`)
- ~~`/reset` CLI~~ — done locally (live-verified wipe)
- ~~Lumen world-knowledge~~ — done locally
- ~~Preference notable_fact merge~~ — done locally
- ~~Background chunk synth~~ — done locally
- ~~Studio `reset-memories`~~ — done locally
- Retest companion balance: Hello → self-intro + optional light question; name share without interview stack; no endless probing
- Commit + push leftovers + Lumen tone passes when retest accepted
- Then Phase 3 STT after user accepts Phase 1–2 as solid

### Later
- Optional `/clear` alias for `/reset confirm` if users keep typing bare "clear"
- Further Studio polish (delete packs, web edit forms, `trace --root`)
- Phase 3 — World Awareness (STT → vision → context)
- Phase 4 — Embodiment
- Phase 5 — Interactive experiences

*Session end (companion balance) — next: retest intro/curiosity, then commit; Phase 3 after accept.*

*Session start (status) — Phase 3 STT still the recommended next major workstream.*

*Session end (commit) — Phase 3 STT recommended next; refine shipped.*

*Session end — Future Ideas unchanged.*
