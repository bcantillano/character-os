# 2026-07-16

## Today's Goal

- New-day status check: OpenAI + long-term SQLite persistence already working — clarify what comes next
- Housekeeping: commit leftover demo/journal docs and push `feature/dev`
- Tackle high-priority Phase 1 polish: spoken memory use + incremental persist
- Help user retest spoken recall via CLI
- Commit verified polish; outline next Phase 1 issues
- Continue Phase 1 polish #1: richer trust / familiarity from interpreter signals
- Live OpenAI trust-building conversation + `/state` debrief
- Memory dedupe (semantic match + SQLite cleanup) before trust-scoring tune
- Clarify where “steal/bargain heuristics” live (prompt + code, not character YAML)
- Universal trust scoring: cooperation-with-character vs hostility-toward-character (all characters)
- Live OpenAI retest after universal scoring (alliance / captain offer / family stakes)

**Outcome:** Spoken-memory polish committed (`de29f47`). Trust + dedupe + universal coop-vs-hostility scoring implemented (`32` tests). Live retest passed: cooperative turns raised trust with **no dip** (~0.39→0.49 familiarity ~0.48→0.54); memories stayed clean. Dialogue stayed wary-but-engaged. Polish committed on user request; `de29f47` (+ this commit) may still need push.

## What We Learned

- Phase 1 gate from yesterday holds: successful human conversation + OpenAI path + SQLite long-term memory
- Spoken recall fails when memory is only soft flavor in the response prompt — speech needs an explicit “known person” block plus recall-question detection
- Incremental persist fits cleanly with a dirty-id set on `MemoryStore`: load clean from DB, mark dirty on add/update/decay, upsert only dirty rows
- Tick decay should update the in-memory store (and mark dirty) rather than decaying SQLite alone — otherwise cache and DB diverge
- `character-os` is not a global command — run from the repo after `source .venv/bin/activate` (or `python -m character_os.cli.chat …`). Do not paste markdown `#` comment lines into zsh
- Human OpenAI retest confirmed speech-memory polish: name appears in spoken dialogue, not only in `(thoughts)`
- Blind familiarity `+0.02` and never-updating `user_trust` made relationship state decorative — intent/tone/signals need per-turn deltas with caps
- Prefer interpreter `trust_delta` / `familiarity_delta` when present; keyword heuristics cover stub / missing numbers
- Live trust chase (earlier): steal/weapons **−** before fix; compass pledge largest +
- Shared risky plans were mis-scored as hostility; correct frame is cooperation *with* vs hostility *toward* the character
- Concrete trust tokens and putting the character in control (grab the map, be captain) raise trust without needing pirate-specific rules
- Post-fix live retest: alliance/leadership/family bonding → trust **0.39→0.49**, familiarity **0.48→0.54**, no negative blips; `/state` memories clean (dedupe holding)
- At trust ~0.5 Redbeard still interrogates loyalty — believable for a cautious character; optional later: soften late-trust interrogation in response prompts
- Drive `trust` lags `user_trust`; curiosity/excitement still peg at 1.0
- Exact-string `find_similar` insufficient — semantic keys + containment + dedupe on load
- Scoring lives in interpreter prompt + `relationship.py`, not character YAML

*Session end — no further learnings after live retest debrief.*

## Architectural Decisions

*Prior locks stand.* Spoken-memory / incremental-persist choices remain (committed in `de29f47`).

Trust/familiarity + memory dedupe + universal scoring (local, not yet committed):

1. Interpreter prompt rules are character-agnostic: raise trust for help/alliance/shared plans; lower only for hostility toward *this* character
2. `relationship.py` detects cooperation vs hostility-toward-character; drops generic crime/violence keywords from automatic negatives
3. Safety net: if cooperation and not hostility, clamp negative `trust_delta` up to at least `max(heuristic, 0.03)`
4. Still engine-global (same for all characters) — not per-YAML rules

*Session end — no new architectural decisions from the live retest.*

## Problems

- ~~Housekeeping push/commit~~ — done (`5682d43`)
- ~~Spoken memory soft-prompt gap~~ — mitigated, live-verified, committed (`de29f47`)
- ~~Persist rewrites all facts every turn~~ — mitigated via dirty-flag incremental upserts
- ~~`character-os: command not found` from home dir~~ — expected without venv; launch from project + activate
- ~~Spoken-recall live verification~~ — passed (name spoken: Byron)
- ~~Blind familiarity / unused trust~~ — mitigated; live `/state` confirms movement
- ~~Duplicate memory rows in local SQLite~~ — deduped; live retest showed clean list
- ~~Shared scheming scored as trust damage~~ — mitigated and live-verified (coop raises trust, no dip)
- Curiosity/excitement saturate at 1.0 from flat per-message nudges
- No forget/archive policy yet (delete when importance ≈ 0)
- ~~Trust + dedupe + scoring tune uncommitted~~ — committing with this journal update
- `de29f47` + follow-up polish may still need push to origin
- Optional: late-trust dialogue still very interrogative (feel, not scoring bug)

*Session end — open items above unchanged.*

## Future Ideas

- Push trust + memory-dedupe + scoring polish when ready
- Soften or gate flat curiosity/excitement nudges so drives stay meaningful
- Optional: soften late-trust interrogation in response/thoughts prompts once trust/familiarity is mid-range
- Forget/archive low-importance facts
- Optional later: per-character relationship scoring overlays in YAML (not needed for this fix)
- Optional: short README “How to run” snippet to avoid PATH confusion
- Optional stage debug logging; brain module split
- Phase 1b voice (TTS) after further polish confidence

*Next: push when requested; then drive-nudge / late-trust dialogue polish or Phase 1b.*
