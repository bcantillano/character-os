# 2026-07-16

## Today's Goal

- New-day status check: OpenAI + long-term SQLite persistence already working — clarify what comes next

**Outcome:** Agreed Phase 1 core loop is working (live OpenAI Redbeard + durable memory). Next priorities ordered: (1) housekeeping push/commit leftovers, (2) Phase 1 believability polish (spoken memory use, incremental persist, trust updates), (3) Phase 1b voice only after text quality is solid. No code written this session.

## What We Learned

- Phase 1 gate from yesterday holds: successful human conversation + OpenAI path + SQLite long-term memory
- Repo still has catch-up work: `feature/dev` is 1 commit ahead of origin (`c29fdb9`); demo transcript + journal updates after that commit remain local/uncommitted
- Highest-impact polish from the demo debrief: spoken dialogue should use known names/facts as reliably as internal thoughts do
- Persistence write amplification (upsert all facts every turn) is the first scaling concern once memory grows — not urgent for short demos

## Architectural Decisions

*No new architectural decisions today.* Prior locks stand (event-driven lifecycle, OpenAI via abstract `llm/`, `gpt-4o-mini` for development, emotional drives, state-only ticks, long-term memory ≠ conversation state).

*Recommended sequencing (not yet locked as build work):* housekeeping → believability polish → Phase 1b TTS.

## Problems

- `c29fdb9` not pushed to origin yet
- Uncommitted local docs: `docs/demos/2026-07-15-first-redbeard-conversation.md`, updates to `docs/engineering-journal/2026-07-15.md`
- Spoken lines may omit known names while thoughts include them
- Persist path still rewrites all loaded facts each turn
- Local DB may still hold pre-dedupe duplicate memory rows
- No forget/archive policy yet

## Future Ideas

- Push `c29fdb9` and commit demo/journal leftovers
- Tighten spoken-dialogue use of long-term memory (names, preferences)
- Incremental persist; forget low-importance facts; optional DB cleanup
- Richer trust/familiarity updates from interpreter signals
- Optional stage debug logging; brain module split
- Phase 1b voice (TTS) only after text believability polish
- Continue debrief punch list from first Redbeard conversation as needed

*Next session: housekeeping first, then choose polish item or continue debrief.*
