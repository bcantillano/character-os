# 2026-07-15

## Today's Goal

- Confirm standing after yesterday's architecture docs commit
- Clarify next implementation steps; begin Phase 1 implementation
- SQLite persistence; discuss DB growth/performance
- Commit/push Phase 1 engine (`477b49e`)
- Status check: mid–Phase 1; success criteria not met
- Continue: live OpenAI Redbeard path + prompt/memory tuning

**Outcome:** Mid–Phase 1 spine + SQLite on `477b49e`. Live OpenAI Redbeard path working. User ran a successful human conversation with the character (Phase 1 believability demo validated qualitatively). Tests `11 passed`. Local SQLite confirmed available.

## What We Learned

- Stub spine is not enough for believability — OpenAI dialogue immediately feels more character-like
- Interpreter labeled-line format works with gpt-4o-mini when prompts demand exact keys
- Memory bugs found in live demo: (1) duplicate facts on each turn; (2) model ignored loosely formatted facts (`name: Byron`) until we canonicalized + injected memory into thoughts and response prompts
- Persistent store alone is insufficient — prompt wiring must make memories unavoidable when relevant
- Local `.env` (gitignored) is the right place for `OPENAI_API_KEY`
- Machine already has SQLite: `/usr/bin/sqlite3` 3.51.0 and Python `sqlite3` (3.47.2). Character OS uses the stdlib module — no pip install; CLI is optional for inspecting `data/character_os.sqlite3`
- Earlier in the day: event bus, state-only ticks, and uneven DB growth (facts grow; drives/relationships upsert) remain valid baseline knowledge

## Architectural Decisions

*Prior locks from 2026-07-14 stand.* Implementation choices today:

1. Default provider remains `stub` for tests; interactive use can select `--provider openai`
2. `python-dotenv` loads repo-root `.env` at CLI / `create_provider`
3. OpenAI client is reused per `OpenAIProvider` instance
4. Decision engine stays rule-based for now (goal pick via topics); LLM used for interpret / thoughts / response
5. Long-term facts are deduped by normalized content; duplicates boost importance instead of inserting new rows
6. Notable facts like `name:` are canonicalized to declarative sentences before storage
7. Tick interval 30s; SQLite for durable state; conversation state session-only

*No new architectural decisions from the SQLite install check.*

## Problems

- ~~OpenAI path not smoke-tested~~ — done live against gpt-4o-mini
- ~~Memory not used in dialogue despite being stored~~ — mitigated via prompt injection + fact canonicalization (validated on retest)
- ~~Need to install SQLite?~~ — already present; not a blocker
- Brain subsystems still folded into orchestrator/helpers
- Persistence still upserts all loaded facts every turn (write amplification)
- No forget/archive policy yet
- ~~Phase 1 success criteria not validated~~ — user reports a successful live conversation
- Existing default DB (`data/character_os.sqlite3`) may still hold pre-dedupe duplicate rows until cleared or migrated
- Brain module split and persistence write-path hardening still deferred

## Future Ideas

- ~~Successful human Redbeard conversation~~ — done (user-reported)
- Debrief conversation questions (next)
- Split brain modules
- Incremental persist (changed facts only)
- Richer relationship trust updates from interpreter signals
- Anthropic / Ollama providers
- Phase 1b voice only after Phase 1 believability gate

*Unchanged — next: longer human demo, then commit when ready.*
