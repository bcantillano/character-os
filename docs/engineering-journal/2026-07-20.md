# 2026-07-20

## Today's Goal

- Live retest companion balance after reset; fix interview-mode relapse if needed
- Agent OpenAI probe loop: converse with Lumen, score leaks, iterate prompts until clean

**Outcome (retest):** First hello intro + day question landed. Then every turn asked another question (mind / which task / enjoy team / approach changes) plus coachy filler ("I'm glad to hear", "It's interesting that", "Collaboration can…"). Tightened Lumen pack: question budget (no question if last two Lumen turns already asked), statement-only after they answer/share, ban coach openers; thoughts no longer plan "keep conversation flowing."

**Outcome (agent probe loop):** Ran OpenAI `persist=False` probes v1→v6 (no TTS).

| Version | Result |
|--------|--------|
| v1 | Cooldown OK (1 question total); coach bans failed ("it's good to hear", "it sounds like") |
| v2 | Work shares clean; turn 2 re-asked + re-intro |
| v3 | Cooldown OK; only re-intro left on turn 2 |
| v4 | **Work script CLEAN**; food script re-asked day after name |
| v5 | Work CLEAN; food prefs OK but recs used "How about…?" |
| v6 | **Food script CLEAN** (spicy list, no How about / no late ?) |

Final rules locked: question cooldown, no re-intro, no question-back when asked how you are, statement-after-share/name, ban coach openers, food lists as plain statements.

*Session end — agent probe loop closed; work+food scripts CLEAN; commit still pending. No further goals this turn.*

## What We Learned

- "One question every few turns" was too soft — model treated each answered question as license to ask the next; need an explicit cooldown (no question if a recent Lumen turn already asked)
- Sharing work details should trigger concrete acknowledgment, not soft-coach analysis + another probe
- Live probe loop beats single-shot retests: each failure mode (re-intro, day re-ask after name, "How about" food lists) needed its own example
- Answering "How is yours?" often triggered a reciprocal new question unless banned explicitly
- Food-rec turns still slip "How about…?" even when spicy prefs are correct — ban + statement-only list example required

*Session end — probe-loop learnings above; What We Learned unchanged beyond those notes.*

## Architectural Decisions

- Keep companion balance as pack-prompt rules: question cooldown + statement-after-share; still no pipeline change
- Agent probe harness: ephemeral OpenAI session, fixed scripts, automated scores (question marks, banned phrases, re-intro, spicy-in-recs)

*Session end — Architectural Decisions unchanged beyond probe harness practice (pack-prompt only).*

## Problems

- Leftovers + Lumen tone stack still local/uncommitted (now includes probe-tightened prompts)
- Thoughts still occasionally soft ("foster trust", "encourage engagement") — non-blocking vs spoken replies
- Phase 3 still waiting on Phase 1–2 accept

*Session end — Problems unchanged; spoken CLEAN on probe scripts; commit + user accept still open.*

## Future Ideas

### Immediate
- ~~Retest anti-interview cooldown on work/share turns~~ — agent probes CLEAN (v4 work, v6 food)
- Commit leftovers + Lumen tone/probe passes when user asks
- Optional user live chat confirm with TTS

### Later
- Phase 3 STT after Phase 1–2 solid
- Optional `/clear` alias; further Studio polish
- Persist a small `scripts/probe_lumen.py` harness if probe loops become routine

*Session end — Future Ideas unchanged; next: commit when ready; Phase 3 after accept.*
